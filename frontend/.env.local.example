# Copy this file to .env.local and fill in your values
# Get Supabase values from: supabase.com → your project → Settings → API

SUPABASE_URL=https://yqjxcqpgapzbqeizjqns.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...   # Settings → API → service_role (secret, server-only)

# Generate strong random values for these — never commit real values
JWT_SECRET=replace-with-strong-random-secret
JWT_REFRESH_SECRET=replace-with-strong-random-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-strong-password
